import type { Metadata } from 'next';
import LockScreenPage from './LockScreenPage';

export const metadata: Metadata = { title: 'Lock Screen 2' };

const LockScreen2 = () => {
	return <LockScreenPage />;
};

export default LockScreen2;

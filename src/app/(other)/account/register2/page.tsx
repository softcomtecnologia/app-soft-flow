import type { Metadata } from 'next';
import RegisterForm from './RegisterForm';

export const metadata: Metadata = { title: 'Register 2' };

const Register2 = () => {
	return <RegisterForm />;
};

export default Register2;
